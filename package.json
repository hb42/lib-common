{
  "name": "@hb42/lib-common",
  "version": "1.0.2",
  "copyright": "Copyright (c) 2018-2019 Hartmut Bauer",
  "description": "Common objects for client + servers",
  "main": "lib/index.js",
  "typings": "./lib",
  "scripts": {
    "clean:lib": "./node_modules/.bin/rimraf lib",
    "clean:tmp": "./node_modules/.bin/rimraf tmp",
    "clean": "npm run clean:lib && npm run clean:tmp",
    "build": "npm run clean && tsc",
    "make": "npm run build && npm --no-git-tag-version version prerelease --preid=beta && npm pack",
    "release": "npm run build && npm --no-git-tag-version version patch && npm pack"
  },
  "author": "hb",
  "license": "MIT",
  "repository": "hb42/lib-common",
  "publishConfig": {
    "registry": "http://e077npm"
  },
  "peerDependencies": {},
  "devDependencies": {
    "copyfiles": "^2.1.0",
    "rimraf": "^2.6.3",
    "tslint": "^5.12.1",
    "typescript": "^3.2.4"
  }
}
